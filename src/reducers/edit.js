import {
} from 'src/actions/edit';

const initialState = {
  // ici l'état initial

};

const edit = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return { ...state };
  }
};

export default edit;
